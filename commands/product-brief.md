---
description: Write a product brief (PM-to-engineering handoff) for a feature
---

Use the product-manager agent. The user wants to write a product brief. Load the `product-brief` skill and follow its workflow. Use `$ARGUMENTS` as the feature name or problem description to brief.
