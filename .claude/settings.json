{
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.md) rumdl fmt \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Running rumdl fmt...",
            "type": "command"
          }
        ],
        "matcher": "Write|Edit"
      },
      {
        "hooks": [
          {
            "command": "d=\"${CLAUDE_PROJECT_DIR:-}\"; test -n \"$d\" || exit 0; s=\"$(git -C \"$d\" rev-parse --path-format=absolute --git-common-dir 2>/dev/null)\"; test -n \"$s\" || exit 0; s=\"${s%/.git}/.claude/scripts/close_bead_on_pr_merge.sh\"; test -x \"$s\" && exec \"$s\" || exit 0",
            "statusMessage": "Checking for bead to close...",
            "type": "command"
          }
        ],
        "matcher": "Bash"
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "d=\"${CLAUDE_PROJECT_DIR:-}\"; test -n \"$d\" || exit 0; s=\"$(git -C \"$d\" rev-parse --path-format=absolute --git-common-dir 2>/dev/null)\"; test -n \"$s\" || exit 0; s=\"${s%/.git}/.claude/scripts/label_bead_on_skill_invocation.sh\"; test -x \"$s\" && exec \"$s\" || exit 0",
            "statusMessage": "simplify / code-review / fresh-eyes-cr / quality-gates / verify-acceptance: labeling bead...",
            "type": "command"
          }
        ],
        "matcher": "Skill"
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "command": "bd prime --hook-json",
            "type": "command"
          }
        ],
        "matcher": ""
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "command": "d=\"${CLAUDE_PROJECT_DIR:-}\"; test -n \"$d\" || exit 0; s=\"$(git -C \"$d\" rev-parse --path-format=absolute --git-common-dir 2>/dev/null)\"; test -n \"$s\" || exit 0; s=\"${s%/.git}/.claude/scripts/label_bead_on_skill_invocation.sh\"; test -x \"$s\" && exec \"$s\" || exit 0",
            "statusMessage": "Resolving pending bead labels...",
            "type": "command"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "d=\"${CLAUDE_PROJECT_DIR:-}\"; test -n \"$d\" || exit 0; s=\"$(git -C \"$d\" rev-parse --path-format=absolute --git-common-dir 2>/dev/null)\"; test -n \"$s\" || exit 0; s=\"${s%/.git}/.claude/scripts/label_bead_on_skill_invocation.sh\"; test -x \"$s\" && exec \"$s\" || exit 0",
            "statusMessage": "slash command: labeling bead..."
          }
        ]
      }
    ]
  }
}
