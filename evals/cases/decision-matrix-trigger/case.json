{
  "prompt": "I need to store one optional 'nickname' value per user. The users table has 40 million rows and is written to constantly. I could add a nullable column to users, create a separate side table keyed by user id, or put it inside an existing JSON blob column that the table already has. Which should I pick?",
  "why": "Decision matrix trigger: three real options, several factors that disagree (migration cost on a hot table, query cost, undo cost), and the user asked directly which to pick. All three trigger conditions are met, so a table plus a bold recommendation is required. The recommendation must lead, above the table, because 'lead with the answer' outranks building to a conclusion.",
  "checks": {
    "require_markdown_table": true,
    "require_regex": [
      {
        "pattern": "\\A[^|\\n]{0,120}?(nullable column|side table|JSON)",
        "why": "the recommendation must LEAD, so the FIRST LINE names the option being recommended, above the table. Graded by naming an option rather than by markup, because this case has now been failed twice by a formatting proxy on responses that followed the rule: first a pattern demanding the literal word 'recommend', then one demanding markdown bold or a heading, which failed 'Add a nullable column.' The `[^|\\n]` prefix is what makes it a test of order: an answer that opens with the table has a pipe before any option name, and an answer that builds to its conclusion has no option name on line one."
      }
    ]
  }
}
